.sub__groups__list
  - current_user.groups.each do |group|
    .sub__group
      = link_to group_messages_path(group) do
        .sub__group__name
          = group.name
        .sub__group__message
          メッセージはまだありません。
